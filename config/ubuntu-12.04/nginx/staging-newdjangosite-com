server {
	listen   80;
	server_name  staging.newdjangosite.com;

	access_log  /var/log/nginx/localhost.access.log;

	location / {
		fastcgi_pass unix:/var/fastcgi/newdjangosite-staging.sock;
		fastcgi_param PATH_INFO $fastcgi_script_name;
		fastcgi_param REQUEST_METHOD $request_method;
		fastcgi_param QUERY_STRING $query_string;
		fastcgi_param CONTENT_TYPE $content_type;
		fastcgi_param CONTENT_LENGTH $content_length;
		fastcgi_param HTTPS $https;
		fastcgi_pass_header Authorization;
		fastcgi_intercept_errors off;
	}

	location /static/ {
		root   /var/www/newdjangosite-staging;
	}
}

#	listen   80;
#	server_name  staging.newdjangosite.com;
#
#	access_log  /var/log/nginx/localhost.access.log;
#
#	location / {
#		rewrite ^(.*) https://staging.newdjangosite.com$1;
#	}
#
#}
#
#server {
#	listen 443;
#	server_name  staging.newdjangosite.com;
#	ssl on;
#	ssl_certificate /etc/nginx/ssl/staging.newdjangosite.com.crt;
#	ssl_certificate_key /etc/nginx/ssl/staging.newdjangosite.com.key;
#
#	access_log  /var/log/nginx/localhost.access.log;
#
#	location / {
#		fastcgi_pass unix:/var/fastcgi/newdjangosite-staging.sock;
#		fastcgi_param PATH_INFO $fastcgi_script_name;
#		fastcgi_param REQUEST_METHOD $request_method;
#		fastcgi_param QUERY_STRING $query_string;
#		fastcgi_param CONTENT_TYPE $content_type;
#		fastcgi_param CONTENT_LENGTH $content_length;
#		fastcgi_param HTTPS $https;
#		fastcgi_pass_header Authorization;
#		fastcgi_intercept_errors off;
#	}
#
#	location /static/ {
#		root   /var/www/newdjangosite-staging;
#	}
#}

